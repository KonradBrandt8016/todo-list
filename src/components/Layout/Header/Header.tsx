import { FC } from 'react';
import React from 'react';
import './_styles_header.scss';

interface HeaderProps {}

const Header: FC<HeaderProps> = ({}) => {
  return <header className='header'>Header</header>;
};

export default Header;
